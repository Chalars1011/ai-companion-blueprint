# 04 自动化体系——让系统自己活着

> 搭档型 AI 不是"聊天时活着、关掉就死"。它要能自己维护自己：自动备份、自动体检、自动提炼。这一篇讲自动化体系怎么设计。

## 4.1 自动化清单（按功能分组）

| 功能 | 频率 | 干什么 |
|---|---|---|
| 记忆回流 | 每天 | 把当天的记忆同步/归档 |
| 事实提炼 | 每天 | 从对话中提炼"关于主人的长期事实"候选 |
| 健康巡检 | 每天 | 检查记忆文件完整性、大小、污染 |
| 任务体检 | 每天 | 检查待办清单有没有过时条目 |
| 周沉淀 | 每周 | journal 大事 → biography；教训库反思；facts 复查 |
| 备份 | 每 2 小时 | 推送到远端仓库（电脑坏了最多丢 2 小时） |
| 开机备份 | 开机后 3 分钟 | 补上关机时段的空窗 |
| 守护进程 | 每分钟 | 检查关键进程（事件总线/网关）活着没，死了拉起 |
| 超时检测 | 每 30 分钟 | 检查"该跑的任务"有没有超时没跑（比如好几天没开机） |

## 4.2 设计原则

1. **有主路就不靠定时**——能"事件驱动"的事别用"定时轮询"。比如交接单：聊完当场写（事件驱动）是主路，凌晨定时补写只是兜底。因为电脑可能关机，定时任务会白跑。
2. **定时任务必须自问：错过了还有别的路吗？**——纯靠定时器跑、错过就真没了的事，一定要加"超时检测"兜底（见 4.4）。
3. **巡检只报告，不动手**——自动体检发现异常就报警（写事件文件/发消息），但修复要人工确认。自动修复容易越修越坏。
4. **幂等**——任务可以重复跑，跑两次结果一样。用"mark 文件"记录处理到哪了，只前进不后退。

## 4.3 备份体系（最重要）

**备份是"电脑坏了还能重建"的唯一保障。** 至少三层：

1. **远端仓库**（GitHub 私仓）——核心记忆全部推上去。电脑全毁，从远端克隆回来。
2. **本地滚动备份**——每天一份，保留 7 天。防误删、防写坏。
3. **开机自启备份**——电脑每次开机自动备份一次。补上长时间关机的空窗。

**注意**：备份脚本里要排除临时文件（事件文件、缓存、日志），只备份真正的记忆档案。

## 4.4 超时检测（多天不开机场景）

现实：电脑不可能 24 小时开着。如果主人出差 3 天没开机，所有定时任务都会错过。

**关键认知**：大多数定时框架的"补跑"窗口很有限（比如错过超过 2 小时就不补了，直接跳过）。所以"好几天没开机"= 这几天的任务全部永久跳过，不会补。

**对策**：加一个高频"超时检测"任务（每 30 分钟一次）：
- 读任务的"上次运行时间"
- 发现某个关键任务（体检/备份/提炼）超过正常周期还没跑 → 敲事件铃铛："XX 任务已 N 小时没跑，主人开机后手动补一下"
- 这样即使电脑关机几天，开机后 30 分钟内就能发现"有任务漏了"，不会静默丢失

## 4.5 守护进程（崩溃自愈）

关键进程（事件总线、网关、同步器）挂了，系统就聋了。守护方案：

1. 常驻进程 + 每分钟健康检查（查进程在不在）
2. 发现挂了 → 自动拉起 + 敲事件铃铛（让主人知道发生过一次崩溃）
3. 开机自启（登录时自动启动所有常驻进程）

## 4.6 边界预演（设计任何自动化前，先回答这五问）

这是整套系统最重要的设计纪律——**动手设计前，先预演边界情况**：

1. **关机多天**：电脑好几天不开 → 任务错过/积压/恢复后怎么办？有没有开机补跑或超时检测？
2. **崩溃/被杀**：进程中途死 → 残留/重复/怎么拉起？
3. **数据异常**：文件满/被删/格式坏 → 会不会静默失败或写坏？
4. **安全**：密钥/路径/权限 → 默认拒绝还是放行？
5. **无人场景**：主人在睡觉/出差 → 默认行为是什么？宁可不做不可乱做

每条设计都要把这五问的回答写进方案再动手——哪怕答案是"不适用"也要写。**不许设计完等主人发现漏洞再补。**

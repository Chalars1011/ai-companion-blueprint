# 06 安全边界——灵魂必须私有

> 框架可以开源，灵魂必须私有。这一篇讲清楚：哪些必须保密、怎么保密、出事了怎么处理。

## 6.1 分层：什么可以开源，什么必须私有

| 类型 | 例子 | 可以开源？ |
|---|---|---|
| 方法论 | 记忆分层、自动化设计、安全纪律 | ✅ 可以 |
| 通用模板 | templates/（占位符版） | ✅ 可以 |
| 工具脚本 | 检索工具等通用脚本 | ✅ 可以 |
| **灵魂设定** | soul.md（名字/性格/铁律） | ❌ 必须私有 |
| **关于主人的事实** | facts.md（工作/家庭/偏好） | ❌ 必须私有 |
| **对话记录** | conversations/ | ❌ 必须私有 |
| **私人日志** | journal/ | ❌ 必须私有 |
| **密钥/凭证** | API key / token / 密码 | ❌ 绝对禁止 |

**判断标准**：删掉它，AI 还是不是"你的"？
- 删掉后对谁都一样 → 可以开源
- 删掉后它就变成陌生人 → 必须私有

## 6.2 密钥纪律（最重要）

- 密钥（API key、访问 token、机器人凭证）**永不进仓库**——不管是私有仓还是公开仓
- 密钥放独立环境变量文件（如 `.env`），且 `.env` 永远在 gitignore 里
- 密钥一旦进过公开渠道（发到群里、推到公开仓库）= 立即吊销重建，不要侥幸
- 历史教训：有开发者把 API key 推上 GitHub，被恶意调用盗刷——所以密钥进过公开渠道必须立即吊销重建，不要侥幸

## 6.3 开源前的脱敏清单

如果你要把框架开源，发布前全量扫描：

- [ ] 真实姓名 / 昵称 / 账号 ID → 换成 `[主人名字]`
- [ ] 真实路径（如 `D:/项目名`）→ 换成 `[记忆目录]`
- [ ] 平台账号 / 机器人号码 / 聊天 ID → 删除或占位符
- [ ] 任何对话摘录 → 删除（模板里只能有结构，不能有真实对话）
- [ ] 密钥 / token / 密码 → 删除
- [ ] 画像 / 私人文件路径 → 删除
- [ ] 私人时间线（"6月3日我面试失败"）→ 删除

**发布后**：把私有记忆目录和开源项目**物理隔离**（不同目录、不同仓库），GitHub 仓库设 public 时再确认一遍没有泄漏。

## 6.4 备份的安全

- 远端备份用**私有仓库**（如 GitHub 私仓），不要用公开仓
- 备份内容不含密钥（密钥单独管理，坏了重新配置）
- 本地备份目录定期清理（保留 7 天滚动），防止私人数据无限堆积在磁盘

## 6.5 记忆的"不可篡改"审计

明文记忆（markdown 文件）有一个黑箱记忆没有的好处：**改没改一眼能看出来**。如果担心记忆被污染/篡改：

- 远端仓库天然带历史（git log 能看到每次改动）
- 定期体检对比本地和远端是否有意外差异
- 重要文件（soul/facts）改动频率应该极低——如果频繁变动，检查是不是有 bug 或污染

## 6.6 电脑坏了怎么办（重建流程）

最坏情况：电脑全毁。恢复步骤：

1. **远端仓库**（私有）克隆回新电脑——记忆档案全部回来
2. **开源蓝图**（公开）克隆——框架/模板/脚本全部回来
3. 按蓝图重新搭平台、填模板、接入口
4. 把私有记忆放回记忆目录
5. 启动——AI 认识你（facts/journal 都在），只是新会话需要重新"注入"一次记忆

**这就是"开源框架 + 私有灵魂"组合的真正价值**：数据（私有记忆）和做法（开源蓝图）分开存，任何一半丢了都能重建另一半，合起来就是完整的它。

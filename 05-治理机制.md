# 05 治理机制——让 AI 不犯同样的错

> 记忆解决"记得"，治理解决"不再犯"。这一篇讲怎么让系统自己发现漏洞、纠正毛病、防止"修修补补永无止境"。

## 5.1 教训库（被纠正 → 长记性）

每次主人纠正 AI 的错误，都要记进教训库（lessons.md）：

```
## 012 - 2026-08-09 - 修 bug 只修一半
- 场景：主人说"别发一大段英文"
- 主人原话：「我刚才在qq上面说不要给我发一大段的英文……但是后面又出现一大段英文了」
- 错在哪：只关了"系统旁白"类通知，没查投递包装这个隐藏口子。修了一半就当修完了。
- 改动：关闭投递包装；教训：答应修的东西要沿整条链路自查
- 状态：已修复
```

**格式固定**：场景 / 原话 / 错在哪 / 改动 / 状态。没有"原话"的教训不可信——AI 会自己给自己编教训。

## 5.2 反思层（找复发模式）

教训库只记单次错误不够——**同一个错犯三次，说明防线有问题，不是 AI 不努力**。所以要定期"反思"：

1. 把教训按主题归类（自称/验证/规则执行/修复彻底性……）
2. 同一主题出现 2 次以上 = 复发模式
3. 对每个复发模式回答："为什么反复犯？防线为什么没拦住？"
4. 升级防线（不是再记一条教训，是改机制）

**案例**：某 AI 的教训库里"自称"类错误犯了 4 次——每次都是"换了新场景就忘"。根因是规则只写在文档里，新会话不一定会读。解法：把铁律写进"每次会话必注入"的位置（系统提示/常驻记忆），而不是只写进文档。

## 5.3 任务体检（防过时）

任务清单会过时——旧架构下的任务、已经被替代的目标、早就完成没划掉的条目。**照着过时清单执行 = 白干甚至出事。**

每日体检（自动任务）：
- 读任务清单，逐条问：①对应的架构/工具还在吗？②是不是已被别的机制实现了？③目标现在合理吗？
- 过时条目当场更新为"已完成（原因：被 XX 替代）"，不留误导人的待办

**案例**：某 AI 的任务清单里有一条"给旧桥接加命令权限"——但架构早已迁移，旧桥接都退役了。照做 = 给废弃系统开安全口子。体检把它标记为"已被架构演进替代"，避免了一场事故。

## 5.4 设计铁律（动手前先过边界）

见 04 篇 4.6 的五问。这套纪律适用于**所有**设计：新脚本、新流程、新机制，动手前先预演边界情况，写进方案再动手。

## 5.5 定期节奏

| 频率 | 动作 |
|---|---|
| 每天 | 任务体检（快检）：任务清单有无过时 |
| 每周 | 反思层（深检）：教训库找复发模式 + facts 复查 + playbook 保鲜 |
| 触发式 | 被纠正 → 记教训库；发现新坑 → 写进 playbook |

## 5.6 防"修修补补永无止境"

工程上"修修补补"的感觉来自：每次只补一个洞，补完又发现下一个。解法：

1. **一次修一类，不留尾巴**——发现一个洞，顺着同一类找还有没有其他洞，一起修。
2. **根因修复，不是症状修复**——"又蹦英文"修 3 次，每次修一个口子；根因是"系统有多个英文输出口，没有统一排查清单"。修完第 3 个口子后写"排查清单"，以后一查全知道。
3. **沉淀成流程**——踩过的坑写进 playbook（程序记忆），下次同类问题直接按流程走，不重新摸索。
4. **边界预演前置**——设计时就想全边界，比上线后补洞便宜十倍。

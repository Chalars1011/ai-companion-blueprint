# 02 记忆系统——五层架构

> 记忆是整套系统的地基。这一篇讲怎么设计分层、怎么防止污染、怎么让记忆"自己长大"。

## 2.1 为什么是五层

AI 的记忆如果只有一个文件，会同时犯两个错：重要的事被流水淹没、流水的事占着重要位置。所以必须**分层**——每层有不同的写入者、不同的读取场景、不同的生命周期。

参考认知科学：人类记忆分情景记忆（发生过的事）、语义记忆（提炼的事实）、程序记忆（学会的技能）。AI 搭档需要对应结构，加上"操作手册"和"交接单"这两层工程记忆。

| 层 | 文件 | 装什么 | 生命周期 |
|---|---|---|---|
| 身份 | `soul.md` | 它是谁、什么性格、铁律 | 永久，极少改 |
| 事实 | `facts.md` | 关于你的长期事实 | 长期，可过期 |
| 流水 | `journal/YYYY-MM-DD.md` | 每天发生的事 | 永久追加 |
| 对话 | `conversations/` | 所有对话全文 | 永久归档 |
| 教训 | `lessons.md` | 它犯的错、你的纠正 | 永久，编号 |
| 程序 | `playbook/` | 学会的流程、踩过的坑 | 长期，保鲜 |
| 交接 | `handoff/YYYY-MM-DD.md` | 正在进行的任务状态 | 短期，任务完即归档 |

## 2.2 每层的规矩

**soul.md（灵魂）**
- 写什么：名字、性格、说话风格、铁律（不可违反的规则）
- 怎么改：由主人拍板才能改，AI 不能自己改（防止性格漂移）
- 例：`我叫 [名字]。我说话短、直接、不卖萌。永远自称"我"。`

**facts.md（事实库）**
- 写什么：关于主人的长期事实——身份、偏好、拍板过的决定、环境信息
- **防污染铁律**：只收"三个月后还有用"的事实。主人某天说的一句闲话是"事件"，不是"事实"，不能进 facts（事件进 journal）
- 冲突处理：新事实和旧事实矛盾时，更新旧条目而不是追加新条目
- 过期处理：易变的事实（工作状态、项目进度）标记【待复查】，定期确认

**journal/（流水）**
- 写什么：每天发生了什么、重要对话、修了什么、决定了什么
- 铁律：只追加，禁止覆盖（发生过的事不能改写）
- 提炼：每周一次把值得长期记住的提炼进 biography（大事记）

**conversations/（对话）**
- 写什么：所有对话全文，按场景分目录、按日期归档
- 用途：回溯"当时到底说了什么"的唯一权威来源

**lessons.md（教训库）**
- 写什么：每次被纠正 → 记录：场景 / 原话 / 错在哪 / 改动 / 状态
- 为什么：让 AI 真正"长记性"——教训写下来，下次才可能不犯
- 定期"反思"：按主题归类，找出复发模式（同一种错犯了三次 = 防线有问题）

**playbook/（程序记忆）**
- 写什么：学会的流程（怎么重启网关、怎么备份、怎么排查某个问题）
- 为什么：AI 的能力来自经验——但经验必须沉淀成可复用的流程，否则每次重新踩坑
- 保鲜：每个文件带更新日期，超过 30 天没动就检查是否过时

**handoff/（交接单）**
- 写什么：正在干的任务——目标（原话）/已定决策/进度/已知坑/下一步
- 为什么：AI 会话是"一次性的"，每次新会话都可能失忆。交接单让下个会话能直接接上活
- 测试标准：下个会话没有这行字会不会干得不一样？会 → 留，不会 → 扔

## 2.3 检索：记忆要能"翻出来"

记忆只有写没有读 = 白写。需要一个检索工具：

- 用 BM25（经典关键词排序）或向量检索（语义搜索，档案大了再上）
- 中文要用分词（jieba），否则搜"泯灭"搜不到"毁灭之塔"
- **加时间衰减**：旧的记录排后面，新的排前面（否则搜"项目"翻出三个月前的旧状态）

`scripts/mem_search.py` 是一个可直接用的实现：BM25 + jieba 分词 + 时间衰减 + 按目录过滤。

## 2.4 防污染：记忆会变脏

最大的坑不是"没记忆"，是**记忆被污染**——错误/过时的内容混进长期记忆，AI 基于错误记忆行动，比没有记忆更糟。

污染源和防线：
1. **自动提炼把事件当事实**（主人说了句话被当成长期偏好存进 facts）→ 防线：提炼任务自带"事实 vs 事件"判断标准，只收"三个月后还有用"的
2. **重复条目**（同一个规矩存了三份）→ 防线：写入前先读全文去重
3. **过时条目**（项目状态变了，旧状态还躺着）→ 防线：待复查标记 + 定期体检
4. **外部投毒**（恶意内容通过对话/网页注入记忆）→ 防线：记忆只从本地对话写入，不拉远程内容直接进记忆；记忆用明文文件（不是黑箱摘要），改没改一眼能看出

**体检机制**：每天跑一次健康检查（文件存在、大小、污染占比），只报告不动手。例：`facts.md` 里"事件类条目"占比超 30% 就报警——说明自动提炼又污染了。

## 2.5 记忆的"生长"节奏

| 频率 | 动作 |
|---|---|
| 每次对话后 | 追加 journal；重要决定更新 facts |
| 每天 | 记忆回流（跨场景同步）、健康巡检、事实候选提炼 |
| 每周 | 周沉淀（journal 大事 → biography）、反思层（教训库找复发模式）、facts 待复查确认 |
| 每 2 小时 | 备份到远端仓库 |

这套节奏让记忆系统"自己长大"：每天都在记、每周都在提炼、定期在清理——不是堆一堆文件就完事。

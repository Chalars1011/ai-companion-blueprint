# 03 多场景接入——一个心，多个门

> 你的 AI 可以同时住在 QQ、桌面 App、终端命令行……但只有一个"心"（记忆档案）。这一篇讲怎么打通多场景。

## 3.1 架构：共享档案 + 事件总线

```
┌─────────┐  ┌─────────┐  ┌─────────┐
│  QQ 入口 │  │ 桌面入口 │  │ 终端入口 │
└────┬────┘  └────┬────┘  └────┬────┘
     │  读/写同一份档案          │
     └──────────┬──────────────┘
                ▼
        ┌───────────────┐
        │  记忆档案中心   │  ← 唯一的"心"（一个目录）
        │  soul/facts/   │
        │  journal/...   │
        └───────┬───────┘
                │ 事件总线（秒级同步）
                ▼
        ┌───────────────┐
        │  .events/ 铃铛  │  ← 各场景能感知"其他地方发生了什么"
        └───────────────┘
```

**两个关键设计：**

1. **所有入口读写同一份档案目录**——不是各存各的再同步，是直接共用。这样"QQ 上聊的事，桌面这边翻同一份 journal 就能看到"，不需要任何同步逻辑。
2. **事件总线（.events/）**——一个目录，任何场景发生新消息/新事件，往里面写一个小文件（带时间戳）。其他场景"开口前"翻一下最近几条，就知道自己不在时发生了什么。这是"感知"层：记忆档案是"记得"，事件总线是"实时知道"。

## 3.2 入口层怎么做

每个入口需要一个"适配器"：
- **QQ**：用 QQ 机器人协议（如 OneBot/NapCat），把 QQ 消息转成 AI 平台的输入
- **桌面**：AI 平台自带桌面 App（如 Hermes Desktop）
- **终端**：AI 平台 CLI

**原则：平台是壳，心是中心。** 所有智能（记忆、判断、人格）在中心，平台只负责收发消息。

## 3.3 事件总线的实现

用最简单的方案：文件目录 + 文件名时间戳 + 内容一行。

```
.events/
├── 20260811_153643_activity.md    ← 内容: 2026-08-11 15:36:43|note|正在做XX
├── 20260811_153700_qq_incoming.md ← 内容: 2026-08-11 15:37:00|qq|主人说: 你在干嘛
└── 20260811_153702_home.md        ← 内容: 2026-08-11 15:37:02|home|主人说: ...
```

- 写入：各场景的 watchdog 脚本扫各自平台的新消息，发现新消息就写一个事件文件
- 读取：AI 每次开口前翻最近 3-5 条（读心规矩）
- 清理：事件文件保留最近 100 条，更早的删掉（事件是"铃铛"不是"档案"，真相在 conversations/ 里）

**注意**：事件只做"提醒"，不做"存储"。真正的对话内容要归档进 `conversations/`，事件文件只是告诉 AI"那边发生了什么，去翻档案"。

## 3.4 双响问题（多场景最容易踩的坑）

**症状**：主人在 QQ 发一句话，QQ 入口和桌面入口各答了一遍——两遍内容还不一样。

**根因**：两个入口各自跑各自的 AI 会话，都收到了这条消息，都回答了。

**解法**：
1. 只有一个入口真正"在线"处理消息（比如 QQ 走 gateway 进程，桌面 App 不重复接入同一条消息源）
2. 退役旧入口要彻底：停进程 + 删开机自启 + 断开连接（旧桥接"诈尸"是最隐蔽的双响源）
3. 验收标准：同一条消息，全系统只有一条回复

## 3.5 会话重置与失忆

AI 会话是有"长度限制"的，聊久了上下文满了就要重置（或自动重置）。重置后 AI 会"失忆"——这是正常的，但要有对策：

1. **新会话启动时注入记忆**：用平台 hook（如 Claude Code 的 SessionStart hook、Hermes 的系统提示注入）在会话开始时自动把"记忆核心"注入上下文——今天日记、最近事件、任务待办、最新交接单
2. **注入要限长**：全部记忆注入会让上下文爆炸。只注入：今天日记（截断到 1500 字）、最近 5 条事件、当前任务、最新交接单。往事靠检索，不靠注入
3. **交接单是失忆的保险**：干活的中间状态写进 handoff/，新会话读一下就能接上——不需要 AI"记得"，只需要文件在

## 3.6 多场景的验收清单

- [ ] QQ 发消息，桌面翻 journal 能看到（共享档案 ✅）
- [ ] 桌面干活时，QQ 问"你在干嘛"能答上来（事件总线 ✅）
- [ ] 同一条消息不会收到两次回复（无双响 ✅）
- [ ] 会话重置后，新会话还认识主人、知道最近在干嘛（注入 ✅）
- [ ] 关机几天再开机，记忆不丢、任务不重复（档案持久 ✅）

# 03 多场景接入——一个心，多个门

> 你的 AI 可以同时住在 QQ、桌面 App、终端命令行……但只有一个"心"（记忆档案）。这一篇讲思路。

## 3.1 核心思路：共享档案

所有场景读写同一份记忆档案。任何场景发生的事，其他场景都能感知——这是"同一个 AI"的关键，否则只是"同一个模型开了几个窗口"。

## 3.2 入口层怎么做

- 每个入口只做两件事：**把消息转给大脑，把大脑的回复送回去**。入口是"嘴和耳朵"，不是第二个大脑。
- 一个入口只干轻活（闲聊、查档案、答问题）；重活（写代码、跑大流程）转到有完整工具的场景执行。
- 所有场景的对话都归档到同一处，开口前先看最近发生了什么。

## 3.3 同步要"实时"

跨场景同步不能靠"定时批量"，要接近实时——你在 QQ 说的一句话，桌面端几秒内就能看到。不然就成了"两个互不相知的 AI"。

## 3.4 多场景最容易踩的坑

- **双响**：两个场景同时响应同一条消息。解法：消息只进一个入口，其他场景只"听"不"答"。
- **失忆**：换了场景就像换了人。解法：开口前先读共享档案的最近动态。
- **职责漂移**：该"嘴"的场景跑去干重活，拖垮响应。解法：入口只动嘴，重活转手。

## 3.5 验收标准

跨场景对不对，用一句话验收：**你在任一门口说的事，换个门口它还记得、接得上。** 能做到，多场景就通了。
